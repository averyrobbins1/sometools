# packages <- c("lemon", "dplyr", "thing", "yours", "ggplot3")
#
# package_name <- glue::glue("{package_name},")
#
# package_name <- glue::glue("{package_name},")
#
#
# packages %>% walk(remember_package)
#
#
# lines <- read_lines("packages.R")
# lines[1]
#
# str_detect(lines, pattern = "lemon") %>%
#   as.character() %>%
#   str_detect("TRUE")
#
# if (str_detect(lines, pattern = "lemon")) {
#   print("Do this")
# } else {
#   print("That")
# }
